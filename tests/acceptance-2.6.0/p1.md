# P1 文件状态与基线验收

2026-09-07，P0 提交 48e1ece 已推送。新增 project-state.md、最小状态模板和只读 project_check.py；保存仍受执行契约控制。当前采用稿、确认快照、探索稿分开；先留旧正文再更新，恢复时读原文。上游改变通过直接和间接依赖报告 stale，保持无关片段可用；不重写 Prompt 或刷新旧审阅。

资产登记模板从上传序号改为稳定 ID；这是批准提案明确要求的唯一保护清单例外。test_acceptance_255.py 保留其历史哈希与解释，仅该项转为专门语义测试；其他 26 项原保护哈希仍全通过。Prompt 编译、情绪描述、库、生产预检均未变。

验证：完整回归通过，131 项 unittest，42 个 shell PASS 标记（含汇总）。9 项新增测试覆盖当前稿恢复、草稿不自动采用、不可变确认快照、未登记外部修改、传递失效、上传重排只影响相关 clip、依赖环/缺失节点、非 Git 快照与篡改、嵌套目的目录拒绝。

真实基线从本仓库 v2.5.5 / e05da76 导出并建立独立快照；manifest SHA-256：b5bcbe65c212536af6202e8d15327adb42639e76d4f96925206de66f477c8f35。manifest 用于内容完整性，版本归属由来源标签与提交共同记录。P3 将另记录只清理流程与完整方法配置。

限制：project_check 是只读检查，不负责自动写稿、确认真实性或创作质量；保存操作按明确文档执行，检查器识别中断造成的不一致。历史项目未迁移时不能伪造已确认状态。
