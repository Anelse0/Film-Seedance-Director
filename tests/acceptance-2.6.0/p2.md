# P2：方法候选与正文通读

实现 `story-development.md` 的条件式正文实验，接入 S3b 与创作回路；多集追踪前集后果，观察性与开放结尾不强制成长。模板正文前置，不新增必填创作表；S4 有序表演块原文保持不变。

`assemble_script.py` 验证指定阅读顺序、正文提取、台词逐字保留、源文件哈希、歧义拒绝和禁止覆盖输入。5 项新测试；全量 136 项 unittest 与 42 个 shell PASS，退出 0。其余 26 个历史核心哈希继续通过；asset-registry 唯一例外见协议。`git diff --check` 通过。

来源已直接核实并在方法文件逐项给出支持范围。P2 工程实现提交为 alpha 候选；完整作品收益与退步证据交由 P3 三配置实验，不把测试通过当作编剧提升。
