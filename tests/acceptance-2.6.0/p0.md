# P0 执行契约验收

2026-09-07，基线 v2.5.5。完成单一执行契约；目标终点与排除项；三种单元；明确保存继承；确认独立；可选表格不再成为 S3c 输入门槛。

原关键词分类器退役。旧自由文本调用返回 2，需要语义理解，不再根据“原文”“短片”“生存”“不要保存”决定操作。新 CLI 校验模型填写的结构化记录。8 条审计原始输入均保留在 fixtures/routing-2.6.0/audit-probes.json；期望解释由维护者填写，不能声称为自动 NLP 验收。

验证：完整回归通过，42 个 shell PASS 标记（含 unittest 汇总）；122 项 unittest 通过。新增边界覆盖：明确不保存覆盖历史自动保存，历史授权继承，确认不建立保存策略，保存不确认，计划不得越过目标/排除项，一场戏可含多个 clip，确认需要证据，错误输入受控失败，概念辅助字段可省略。旧 6 个关键词分类测试由 12 个结构化契约测试替代；原核心回归保留。

未验证：端到端自然语言任务与独立创作效果留给 P3。未调整生产/表演保护区。
